import {
	
} from './types';

import Api from '_/services/api';

const createPlaylist = (data) => async(dispatch, getState) => {
	console.log('creating the playlist!');
};

export default createPlaylist;